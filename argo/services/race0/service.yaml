apiVersion: v1
kind: Service
metadata:
  name: race0
  labels:
    app.kubernetes.io/name: race0
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: race0
  ports:
    - name: http
      port: 80
      targetPort: http
